apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kayvault.fullname" . }}-test-connection"
  labels:
    {{- include "kayvault.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kayvault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
