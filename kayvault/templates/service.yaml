apiVersion: v1
kind: Service
metadata:
  name: {{ include "kayvault.fullname" . }}
  labels:
    {{- include "kayvault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kayvault.selectorLabels" . | nindent 4 }}
